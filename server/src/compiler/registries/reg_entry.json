{
	"$schema": "http://json-schema.org/schema",
	"type":"object",
	"properties": {
		"abstract": {
			"description": "Set to true to make this entry to be only used to be inherited from other entry, and not be registered.",
			"type":"boolean"
		},
		"extends":{
			"description": "A reference to another entry or mixin to inherit all tags from",
			"type":"string"
		},
		"tags": {
			"description": "The list of properties this entry or mixin defines",
			"$ref": "./tags_schema.json"
		},
		"mixins":{
			"description": "A list of mixins to add all tags from",
			"type":"array",
			"items": {
				"type":"string"
			}
		}
	}
}